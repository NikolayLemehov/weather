import { GlobalStyles as GlobalStylesMui } from "@mui/material";

export const globalStyles = (
  <GlobalStylesMui
    styles={{
      body: { margin: 0, padding: 0, fontFamily: '"Roboto", "Helvetica", "Arial", sans-serif' },
      h1: { color: "blue" },
    }}
  />
);
